# B-1 — 사라진 항목을 건너뛰고 패스를 완주시킨다 (fix-seam)

**first-increment** · blocked-by: (none) · plan: `docs/bugfixes/reconcile-vanished-entry-aborts-pass.md`

## 규범

계획이 **유일한 규범**이다. 특히:

- **§구현 ①~⑦** — **의사코드가 아니라 스크래치 프로토타입에서 실제로 컴파일되고 스위트를 통과한 소스**다.
  그대로 옮겨라. 임의로 바꾸지 마라(가시성·lifetime·재수출·시그니처가 전부 컴파일 증거를 갖고 있다).
- **§0(증인 게이트)** — `scripts/f14-witness-gate.sh`의 **스크립트 + 술어×케이스 매트릭스가 SSOT**다.
- **§B-1 acceptance** — 0단계(게이트 `--selftest` → 본 게이트)부터 순서대로.
- **§증인 ID 표** — 이 표에 없는 증인은 **존재하지 않는 것으로 간주한다**.

## 단일 플립 (락)

"스냅샷 이후 **사라진** `.objects` 항목이 패스 전체를 `Err`로 중단시킨다" → "그 항목을 **건너뛰고 패스가 완주한다**".

`flips[]` 2행(같은 하나의 관측 행동에 대한 두 증인):
- `reconcile_pass_survives_an_entry_that_vanishes_after_the_snapshot` (Blob 분기)
- `reconcile_pass_survives_a_temp_that_vanishes_after_the_snapshot` (Temp 분기)

둘 다 symptomToken `PASS ABORTED` · red.sha `ac58bd7` · `--verify-red` 통과.

## 절대 하지 말 것

- **`?`를 그냥 없애지 마라.** `ErrorKind::NotFound`이고 **`symlink_metadata(e.path())`가 부재를 확인**할 때에만
  skip한다. 그 외 모든 io 에러는 **무가공 전파**(B7).
- **`ReconcileStats`에 필드를 추가하지 마라**(`tests/layout_tree.rs`의 전수 `assert_eq!` 3곳이 깨진다 = 두 번째 플립).
- **O1/O2 syscall 순서를 깨지 마라**(예약 이름은 `file_type` **전에** `continue`).
- **characterization 테스트를 약화·삭제·`#[ignore]` 하지 마라**(anti-cheat Blocker). 스위트가 빨개지면
  **테스트가 아니라 구현을 고쳐라.**
- **`Cargo.toml`을 건드리지 마라.** **`unsafe` 0.** **fd 추가 0.**
- **핀 / `fstatat` / `(dev,ino)` / `PinnedDir`을 되살리지 마라**(A안·C안은 인간이 반려했다).
- **소스-문자열 규율(W8/W12)을 되살리지 마라**(라운드 7에서 폐기).

## Definition of done

계획 §B-1 acceptance의 **모든 항목** + `scripts/f14-witness-gate.sh` 통과(0단계) +
회귀 증인 2개 GREEN + characterization **0 failed** + `0 ignored` + clippy/fmt 델타 0.
